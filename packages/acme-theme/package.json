{
  "name": "@acme/theme",
  "version": "0.0.0",
  "scripts": {
    "build": "postcss src/tailwind.css -o dist/tailwind.output.css",
    "dev": "postcss src/tailwind.css -o dist/tailwind.output.css --watch",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "tailwindcss": "3.1.8"
  },
  "dependencies": {
    "autoprefixer": "^10.4.8",
    "postcss": "^8.4.16",
    "postcss-cli": "^10.0.0",
    "postcss-import": "^14.1.0",
    "postcss-mixins": "^9.0.3",
    "postcss-nested": "^5.0.6"
  }
}
